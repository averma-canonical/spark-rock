summary: Initial Spark config
description: |
  A simple Pebble layer included with the underlying ROCK that can be used to start and test the
  Spark instance within.
services:
  spark:
    override: replace
    command: /opt/sbin/spark-daemon.sh --configFile=/etc/spark/spark.yaml
    startup: enabled
    user: spark
